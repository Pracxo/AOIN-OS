# AOIN OS Project Rules

1. Keep all AOIN OS files inside `/Users/damilaremerotiwon/KITEV2/AOIN OS`.
2. Do not create sibling project folders unless explicitly instructed.
3. Do not add secrets to Git. Use `.env.example` for placeholders.
4. Keep implementation instructions in `exchange/implementation-queue.md` or `instructions/`.
5. Keep generated build output in `build/` only when it is intentionally preserved.
6. Prefer small, verifiable changes over broad rewrites.
7. Before deleting or resetting project data, confirm the target scope.
