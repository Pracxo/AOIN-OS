# Generic AION Module Example

This example is a contract-only module package for the AION Module Developer
Kit. It contains no executable code, no provider SDK integration, no external
endpoint, and no domain workflow.

Use it to exercise module package submission, compatibility checks, contract
tests, and certification.

