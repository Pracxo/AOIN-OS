# Build Artifacts

Keep intentional build outputs, release notes, or generated deliverables here.

Temporary compiler output should remain ignored unless it is deliberately promoted into this folder for safekeeping.
