# 0118: v0.2 Proposal Registry Stabilization

## Status

Accepted

## Context

AION-126 created the v0.2 workstream proposal registry and approval queue preview. AION needs a stable proposal and approval queue baseline before implementation approval can be considered.

## Decision

Decision: add v0.2 proposal registry stabilization gate.

Decision: AION-127 does not approve implementation.

Decision: proposal registry and approval queue remain preview-only.

Decision: approval queue item approval remains false.

Decision: future implementation still requires explicit approval records, ADRs, and gate evidence.

Decision: no v0.2 release or tag is created.

## Reason

Reason: AION needs a stable proposal and approval queue baseline before implementation approval can be considered.

## Consequences

Consequence: future workstreams remain blocked until explicit approval records are created.

Constraint: no runtime enablement.

Constraint: no external calls.

Constraint: no credentials/tokens.

Constraint: no sandbox execution.

Constraint: no privileged bypass.
