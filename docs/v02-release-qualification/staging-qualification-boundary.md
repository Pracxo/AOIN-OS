# Staging Qualification Boundary

The future runner may use only read-only Git commands, exact repository scripts, Python standard library and allowlisted Docker CLI argument arrays with `shell=false`. Public egress, DNS, registry login, registry pull, registry push, cloud APIs, external IdP calls, production credentials, production tokens, production databases, production deployment and release creation are disabled.
