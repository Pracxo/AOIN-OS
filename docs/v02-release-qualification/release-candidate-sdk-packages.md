# AION-243 SDK Candidate Packages

The SDK candidate artifacts are built offline from the immutable source
snapshot with Hatchling `1.31.0`.

The runner builds the wheel and source distribution twice and requires matching
SHA-256 fingerprints. The retained artifacts are:

- `sdk/aion_sdk_python-0.2.0rc1-py3-none-any.whl`
- `sdk/aion_sdk_python-0.2.0rc1.tar.gz`

No public package index upload is authorized.
