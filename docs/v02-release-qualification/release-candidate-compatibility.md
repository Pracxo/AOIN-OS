# AION-243 Compatibility Matrix

The compatibility matrix checks Python package metadata, the Linux arm64
candidate image, Brain API import, FastAPI app import, `pip check`, SDK wheel
metadata, SDK import metadata, `aionctl` entry point metadata, Operator Console
bundle integrity, current public API compatibility and the absence of
production deployment.
