# Final Release Authorization

AION-244 closes `AION-242-V02RQ-0003` as consumed by AION-243 and creates `AION-244-V02REL-0001` as the single active authorization after the PASS evaluation.

The new authorization is single-use and allows exactly one annotated `aion-v0.2.0-rc.1` tag, one GitHub prerelease, 24 release asset uploads, and 24 download verifications. It allows zero stable tags, zero stable releases, zero registry pushes, zero public package uploads, and zero production deployments.

The authorization remains active and unconsumed until the post-merge publication step completes and is reconciled by a final PR.
