# AION-243 Candidate Checksums

The candidate bundle includes `metadata/SHA256SUMS`.

The checksum manifest covers unsigned retained candidate files: source archive,
Brain API OCI archive, Brain API artifact manifest, SDK artifacts, Operator
Console bundle, version manifest, content manifest, SBOM, provenance,
reproducibility evidence, compatibility matrix, migration manifest and draft
release notes.

The runner verifies checksums immediately after generation and during live
retention checks.
