# Staging Offline Build

AION-241 may perform at most two local container builds with public network disabled, DNS disabled, `--pull=false`, no registry login, no registry pull, no registry push, no build secrets and no SSH forwarding.
