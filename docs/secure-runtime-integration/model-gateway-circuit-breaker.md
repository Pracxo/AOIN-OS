# Model Gateway Circuit Breaker

Circuit-breaker state is local simulation metadata. It may block future planned provider candidates in deterministic replay, but it cannot create network calls, reset provider failures through background jobs, or override the provider-egress hold.
