# Capability Runtime Audit Provenance

Audit and provenance are append-only, redacted, and bounded. They retain fingerprints and lineage only, not raw prompts, raw model outputs, credentials, tokens, filesystem data, provider payloads, or connector payloads.
