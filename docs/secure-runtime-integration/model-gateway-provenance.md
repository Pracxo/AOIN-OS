# Model Gateway Provenance

Output provenance records deterministic reference-provider identity, request and response fingerprints, validation status, classification, redaction state, and audit lineage. It stores no hidden reasoning, retained prompt body, retained response body, credential, token, provider raw payload, cross-session context, or protected material.
