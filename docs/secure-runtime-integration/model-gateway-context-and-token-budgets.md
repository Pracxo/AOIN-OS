# Model Gateway Context And Token Budgets

Context and token budgets fail closed. AION-233 may enforce bounded context items, context bytes, prompt bytes, input tokens, output tokens, total session tokens, structured schema size, and response bytes.

No policy, risk, guardrail, or approval result can override a budget failure.
