# Capability Runtime Execution Receipts

Future receipts are immutable redacted records containing request lineage, manifest fingerprints, schema fingerprints, policy/risk/guardrail/approval fingerprints, budget decision, sandbox profile, result fingerprint, rollback pointer, audit hash, and provenance hash. They contain no raw prompt, raw model response, credentials, tokens, or external payloads.
