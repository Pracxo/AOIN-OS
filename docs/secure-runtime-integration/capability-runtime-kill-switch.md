# Capability Runtime Kill Switch

The parent kill switch precedes future capability execution. A killed, stale, missing, or mismatched kill-switch state blocks execution before dispatch and records redacted operator review evidence.
