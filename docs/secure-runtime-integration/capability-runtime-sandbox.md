# Capability Runtime Sandbox

The sandbox is deterministic and in-memory. It has no network, DNS, filesystem, process, shell, subprocess, browser, dynamic import, eval, exec, package install, module activation, credential, token, connector, tool, production, source, Git, deployment, or model-training effect.
