# Model Gateway Routing Implementation

Routing is deterministic planning evidence only. Candidates bind provider manifest, model manifest, capability profile, operation, budget fit, output-mode fit, structured-schema fit, circuit-breaker state, estimated tokens, deterministic cost estimate, deterministic latency estimate, provider ID, and model ID.

Routing never performs a provider call, network call, fallback execution, retry execution, or cost charge.
