# Model Gateway Provider Manifests

Provider manifests are immutable, credential-free records. They identify only closed allowlist providers and safe simulation metadata.

A manifest may not contain provider credentials, provider SDK configuration, provider endpoints, network connection state, authorization headers, tokens, or live model sessions.
