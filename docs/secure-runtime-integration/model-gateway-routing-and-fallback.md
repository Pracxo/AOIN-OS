# Model Gateway Routing And Fallback

Routing, fallback, and retry plans are deterministic plans only. They select from closed provider and model allowlists and record estimates for cost and latency.

AION-233 may plan retry and fallback candidates, but automatic routing execution, automatic fallback execution, automatic retry execution, and live provider egress remain disabled.
