# Capability Runtime Rollback

Rollback is local and deterministic. Since AION-235 is authorized only for no-external-effect reference operations, rollback records deterministic compensation state and closes the request without touching production resources.
