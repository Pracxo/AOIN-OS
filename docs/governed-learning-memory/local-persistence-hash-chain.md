# Local Persistence Hash Chain

The local ledger must maintain a global hash chain and per-transaction hash chain. Integrity checks must detect truncation, reordering, previous-hash substitution, transaction replay, and content-envelope substitution.
