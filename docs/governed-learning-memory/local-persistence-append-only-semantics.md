# Local Persistence Append Only Semantics

Knowledge versions, projection records, supersession, retraction, expiry, rollback, and active markers are append-only ledger events. AION-224 must reject hidden updates, hidden deletes, hard deletion, and in-place projection mutation.
