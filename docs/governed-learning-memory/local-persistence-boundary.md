# Local Persistence Boundary

The future store remains local only, outside production memory, disabled by default, and unavailable until AION-224 implementation. Background writers, automatic promotion, belief mutation, network access, runtime approval creation, source mutation, Git mutation, production exposure, v0.2 tags, and v0.2 releases remain disabled.
