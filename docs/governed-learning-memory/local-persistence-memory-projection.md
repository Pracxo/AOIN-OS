# Local Persistence Memory Projection

Semantic, episodic, and procedural projection records may be persisted only inside the isolated local store as projection evidence. Production memory remains unwritten. Belief projection remains a candidate record and cannot create a BeliefClaim.
