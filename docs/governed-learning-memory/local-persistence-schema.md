# Local Persistence Schema

Future schema version 1 must be bootstrapped explicitly, reject unknown user_version values, enforce foreign keys, require hash-chain fields, keep transaction IDs unique, make knowledge identity and version pairs unique, and install update and delete rejection triggers on append-only tables.
