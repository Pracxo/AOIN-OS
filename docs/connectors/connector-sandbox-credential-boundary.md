# Connector Sandbox Credential Boundary

Connector sandbox credential access remains disabled. AION-113 adds connector
credential architecture preview evidence but does not grant sandbox access to
credentials, tokens, secret material, OAuth/OIDC/SAML runtimes, or external
identity bindings.

The sandbox and credential gates are both no-go gates until a later reviewed
implementation milestone changes them.
