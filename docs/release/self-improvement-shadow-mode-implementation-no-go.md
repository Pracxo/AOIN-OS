# Shadow-Mode Implementation No-Go

AION-178 no-go evidence rejects protected controller edits, workflow changes,
dependency changes, migrations, config/kernel/API/SDK runtime changes,
production-auth source changes, prohibited imports, runtime registration,
background execution, source mutation, Git mutation, PR creation, approval
creation, merge, canary, deployment, promotion, model training, v0.2 tags,
v0.2 releases, and movement of `aion-v0.1.0`.
