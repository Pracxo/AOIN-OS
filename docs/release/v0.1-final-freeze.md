# AION Brain v0.1 Final Freeze

## Freeze Purpose

Freeze the AION Brain v0.1.0 local release baseline for AION OS, the Adaptive
Intelligence Orchestration Nexus Operating System. AION means Adaptive
Intelligence Orchestration Nexus.

## Release Target

- Version: `0.1.0`
- Tag preview: `aion-v0.1.0`
- Release posture: local-first, Docker-local verified, dry-run safe by default

## Required Commands

```bash
./scripts/check.sh
./scripts/final-docs-audit.sh
./scripts/rc-check.sh --offline-ok
./scripts/rc-evidence.sh --offline-ok
./scripts/golden-path.sh --offline-ok
./scripts/release-smoke.sh --offline-ok
./scripts/setup-doctor.sh --fast --offline-ok
docker compose config --quiet
docker compose up --build -d brain-api postgres redis nats opa
curl -fsS http://localhost:8080/health
curl -fsS http://localhost:8080/health/ready
./scripts/demo-local.sh --offline-ok
docker compose down
```

## Required Green Gates

- Brain tests
- SDK tests
- lint
- typecheck
- no-domain-drift
- boundary check
- policy coverage
- OpenAPI hygiene
- repo health
- Docker compose config
- Docker health and readiness
- setup doctor
- golden path
- release smoke
- RC gate
- freeze gate dry-run
- release package dry-run
- docs audit
- local demo

## Docker Smoke Expectations

The default local stack must start with Brain API, Postgres, Redis, NATS, and
OPA. `/health` and `/health/ready` must respond successfully before demo-local
evidence is accepted.

## Evidence Pack Expectations

Evidence must include command names, pass/fail status, local source artifact,
and whether failure blocks the release. Evidence must not contain raw secrets,
private provider inputs, private reasoning traces, provider payloads, or domain
module logic.

## Tagging Guidance

Do not tag if any required gate fails. Do not push a tag until operator
sign-off. Do not amend evidence after tagging without a new tag.

## No-Go Conditions

No-go conditions include any failed required gate, production auth claim, full
autonomy, external model calls by default, external notification delivery by
default, extension code loading, capability activation, dynamic route
registration, hard delete, model-generated tool execution, external deployment,
or domain modules in Brain core.

## Sign-Off Checklist

- [ ] `VERSION` equals `0.1.0`.
- [ ] `CHANGELOG.md` and `RELEASE_NOTES.md` are complete.
- [ ] Final docs audit passed.
- [ ] Final verification passed.
- [ ] Freeze script passed in dry-run mode.
- [ ] Docker smoke passed when Docker is available.
- [ ] Operator acceptance checklist is complete.
- [ ] Tag preview was inspected.
- [ ] Worktree is intentional and ready for commit.
