# Epistemic Assessment Runtime Hold

`epistemic_truth_engine_implemented=true`. `epistemic_truth_engine_runtime_enabled=false`. `persistent_assessment_write_enabled=false`. `assessment_database_enabled=false`. `maximum_persistent_assessment_write_batch=0`.

The runtime hold remains active until a future authorization explicitly changes it. AION-212 is the next formal closeout task.
