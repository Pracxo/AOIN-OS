# AION-243 Release Candidate Security Evidence

The candidate build uses the pinned offline Hatchling toolchain, no package
downloads, Docker `--network=none`, Docker `--pull=false`, no registry login,
no registry push, no production credentials and no production tokens.
