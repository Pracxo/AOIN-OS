# Model Gateway Authorization Runtime Hold

The runtime hold keeps provider calls, provider egress, provider SDK use, credentials, tokens, connectors, tools, production runtime, deployment, model training, v0.2 tags, and v0.2 releases disabled.
