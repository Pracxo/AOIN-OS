# Research Contracts

AION-205 implements the `AION-204-KI-0001` controlled research-acquisition core as operator-invoked and runtime-disabled. It adds immutable query, plan, candidate, snapshot, provenance, citation, lineage, budget, incident, diagnostic, and operator-review evidence contracts.

State: `research_plane_state=implemented_operator_invoked_disabled`. `research_runtime_enabled=false`, `network_access_enabled=false`, `public_network_fetch_available=false`, `knowledge_promotion_enabled=false`, and `automatic_belief_creation_enabled=false`.

Acquired content is untrusted evidence only. Source class metadata, repetition, and citations do not verify factual claims, promote knowledge, or mutate cognitive beliefs. AION-206 remains the formal closeout and operator-evaluation task.

## Evidence

- URL canonicalization, allowlists, destination policy, redirect policy, content and charset validation, safe headers, budgets, disabled adapters, in-memory adapters, local fixtures, immutable snapshots, provenance, citations, lineage, diagnostics, and operator review are implemented.
- System HTTP transport is unavailable; public network fetch remains false.
